#include<stdio.h>
int main()
{
	char str[30] ,i,w=0,j;
	printf("ener your string= ");
	gets(str);
	puts(str);
	for(i=0;i<str[i];i++);
	printf("%d\n\n",i);
	for(j=0;j<i;j++)
	{
		if(str[i]==' ');
		w++;
	} printf(" word is= %d",w+1);
}

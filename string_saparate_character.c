//Separate the individual characters from a string
#include<stdio.h>
#include<string.h>
int main()
{
	char str[20],i;
	printf("enter your string = ");
	gets(str);
puts(str);	for(i=0;str[i];i++)
{printf("%c ",str[i]);
	
}

}
